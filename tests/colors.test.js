/* eslint-disable */
import { setMagenta } from '../src/main.js';
import { setCyan } from '../src/main.js';
import { setFirebrick } from '../src/main.js';
import { setSkyblue } from '../src/main.js';
import { setSpringgreen } from '../src/main.js';

test('Color changes to magenta after a click', () => {
  '#test1'.click();
  expect(setMagenta()).toBe('magenta');
});
test('Color changes to cyan after a click', () => {
  '#test1'.click();
  expect(setCyan()).toBe('cyan');
});
test('Color changes to firebrick after a click', () => {
  '#test1'.click();
  expect(setFirebrick()).toBe('firebrick');
});
test('Color changes to skyblue after a click', () => {
  '#test1'.click();
  expect(setSkyblue()).toBe('skyblue');
});
test('Color changes to springgreen after a click', () => {
  '#test1'.click();
  expect(setSpringgreen()).toBe('springgreen');
});
test('Color changes to magenta after a click', () => {
  '#test1'.click();
  expect(setMagenta()).toBe('magenta');
});

test('Color changes to magenta after a click', () => {
  '#test2'.click();
  expect(setMagenta()).toBe('magenta');
});
test('Color changes to cyan after a click', () => {
  '#test2'.click();
  expect(setCyan()).toBe('cyan');
});
test('Color changes to firebrick after a click', () => {
  '#test2'.click();
  expect(setFirebrick()).toBe('firebrick');
});
test('Color changes to skyblue after a click', () => {
  '#test2'.click();
  expect(setSkyblue()).toBe('skyblue');
});
test('Color changes to springgreen after a click', () => {
  '#test2'.click();
  expect(setSpringgreen()).toBe('springgreen');
});
test('Color changes to magenta after a click', () => {
  '#test2'.click();
  expect(setMagenta()).toBe('magenta');
});

test('Color changes to magenta after a click', () => {
  '#test3'.click();
  expect(setMagenta()).toBe('magenta');
});
test('Color changes to cyan after a click', () => {
  '#test3'.click();
  expect(setCyan()).toBe('cyan');
});
test('Color changes to firebrick after a click', () => {
  '#test3'.click();
  expect(setFirebrick()).toBe('firebrick');
});
test('Color changes to skyblue after a click', () => {
  '#test3'.click();
  expect(setSkyblue()).toBe('skyblue');
});
test('Color changes to springgreen after a click', () => {
  '#test3'.click();
  expect(setSpringgreen()).toBe('springgreen');
});
test('Color changes to magenta after a click', () => {
  '#test3'.click();
  expect(setMagenta()).toBe('magenta');
});
